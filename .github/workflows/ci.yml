name: CI

on:
  push:
    branches:
      - development
    paths:
      - 'cmd/**'
      - 'internal/**'
      - 'docs/**'
      - 'Dockerfile'
      - '.env.example'
      - 'go.mod'
      - 'go.sum'
      - 'render.yaml'
      - '.github/workflows/ci.yml'
  pull_request:
    branches:
      - development
    paths:
      - 'cmd/**'
      - 'internal/**'
      - 'docs/**'
      - 'Dockerfile'
      - '.env.example'
      - 'go.mod'
      - 'go.sum'
      - 'render.yaml'
      - '.github/workflows/ci.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ohey-backend-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  go_test:
    name: Go test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd

      - name: Run shared Go test action
        uses: Suuu-sh/Shared/actions/go-test@7f6f9dc12a0b3e34bee54aaf7c60f45dadfb6ba4
        with:
          working-directory: .
          go-version: '1.25'
          test-command: go test ./...
          upload-coverage: 'false'

  go_vet:
    name: Go vet
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd

      - name: Run go vet via shared Go action
        uses: Suuu-sh/Shared/actions/go-test@7f6f9dc12a0b3e34bee54aaf7c60f45dadfb6ba4
        with:
          working-directory: .
          go-version: '1.25'
          test-command: go vet ./...
          upload-coverage: 'false'
